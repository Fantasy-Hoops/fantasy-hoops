import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
    tabs: {
        margin: '2rem auto',
        display: 'table'
    },
    tabPanel: {
        padding: 0
    }
}));