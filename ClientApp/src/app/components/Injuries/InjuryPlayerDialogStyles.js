import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    root: {
        maxWidth: '18rem'
    },
    dialogContent: {
        maxWidth: '32rem',
        padding: '0 !important',
    },
});