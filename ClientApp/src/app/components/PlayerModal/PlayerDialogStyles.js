import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
    root: {
      margin: 'auto'  
    },
    content: {
        width: '90vw',
        maxWidth: '100rem',
        padding: '1rem'
    }
}));