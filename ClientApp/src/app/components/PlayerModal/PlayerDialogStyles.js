import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
    content: {
        width: '83vw',
        maxWidth: '100rem',
        padding: '1rem'
    }
}));